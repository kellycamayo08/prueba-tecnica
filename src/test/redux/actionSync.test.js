import '@testing-library/jest-dom';
import { loginSync } from "../../redux/actions/actionsRegister";
import { typesLogin } from "../../redux/types/types";

describe('Verificar acciones del login sync', () => {
    // test('validar login sincronico', () => {
    //     const email = "liliana@gmail.com"
    //     const password = "123456789"

    //     const loginAction = loginSync(email, password);

    //     expect(loginAction).toEqual({
    //         type: typesLogin.login,
    //         payload: {
    //             email,
    //             password
    //         }
    //     })
    // })
})